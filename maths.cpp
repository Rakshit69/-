#include <iostream>
#include<cmath>

int main(){

    double x = 4.02;
    double y = 3;
    double z;

    // z = std::max(x, y);
    // std::cout << z;
    // z = abs(-3);
    // z = round(x);
    // z = ceil(x);
    // z = floor(x);
    // z = sqrt(9);
    // z = pow(2, 4);
    
    std::cout << z;
    return 0;
}